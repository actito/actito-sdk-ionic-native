import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.actito.sample.user.inbox.app.dev',
  appName: 'sample-user-inbox',
  webDir: 'build',
};

export default config;
